st <- proc.time()
sigma1       <- (t(resid)%*%resid)/(n-1)
end1 <- proc.time()-st
st <- proc.time()
sigma2       <- crossprod(resid)/(n-1)
end2 <- proc.time()-st
sigma1==sigma2
end1*10000
end2*10000
st <- proc.time()
sigma1       <- (t(resid)%*%resid)/(n-1)
end1 <- proc.time()-st
st <- proc.time()
sigma2       <- crossprod(resid)/(n-1)
end2 <- proc.time()-st
sigma1==sigma2
end1*10000
end2*10000
st <- proc.time()
sigma1       <- (t(resid)%*%resid)/(n-1)
end1 <- proc.time()-st
st <- proc.time()
sigma2       <- crossprod(resid)/(n-1)
end2 <- proc.time()-st
sigma1==sigma2
end1*10000
end2*10000
st <- proc.time()
sigma1       <- (t(resid)%*%resid)/(n-1)
end1 <- proc.time()-st
st <- proc.time()
sigma2       <- crossprod(resid)/(n-1)
end2 <- proc.time()-st
sigma1==sigma2
end1*10000
end2*10000
st <- proc.time()
sigma1       <- (t(resid)%*%resid)/(n-1)
end1 <- proc.time()-st
st <- proc.time()
sigma2       <- crossprod(resid)/(n-1)
end2 <- proc.time()-st
sigma1==sigma2
end1*10000
end2*10000
st <- proc.time()
sigma1       <- (t(resid)%*%resid)/(n-1)
end1 <- proc.time()-st
st <- proc.time()
sigma2       <- crossprod(resid)/(n-1)
end2 <- proc.time()-st
sigma1==sigma2
end1*10000
end2*10000
sigma1==sigma2
sigma       <- crossprod(resid)/(n-1)
lm.fit(Y~1)
lm.fit(Y~)
lm.fit(Y)
dim(Y)
lm(Y)
lm(Y~1)
out <- lm(Y~1)
out$call
coef()
?coef
check <- list(coef=c(1,2,3))
coef(check)
con <- list(const = TRUE,
getSE = TRUE)
# Perform some checks for controls
nmsC <- names(con)
con[(namc <- names(control))] <- control
if(length(noNms <- namc[!namc %in% nmsC])){
warning("unknown names in control: ", paste(noNms,collapse=", "))
}
# ----- transform data
lagged_vals <- ts_lagged(Y, p)
p=1
con <- list(const = TRUE,
getSE = TRUE)
# Perform some checks for controls
nmsC <- names(con)
con[(namc <- names(control))] <- control
if(length(noNms <- namc[!namc %in% nmsC])){
warning("unknown names in control: ", paste(noNms,collapse=", "))
}
# ----- transform data
lagged_vals <- ts_lagged(Y, p)
y <- lagged_vals$y
x <- lagged_vals$X
b0 <- matrix(0, p + con$const,1)
phi <- matrix(0, p , 1)
n = nrow(lagged_vals$y)
npar = p
X     <- cbind(1, x)
npar  <- npar + 1
crossprod(X)
t(X)%*%X
?solve
solve(crossprod(X))
crossprod(X,y)
t(X)%*%y
crossprod(solve(crossprod(X)),crossprod(X,y))
solve(t(X)%*%X)%*%t(X)%*%y
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
b0==b
end1
end2
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
b0==b
end1
end2
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
b0==b
end1
end2
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
b0==b
end1
end2
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
b0==b
end1
end2
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
b0==b
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
b0==b
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
b0==b
end1*10000
end2*10000
max(abs(b0-b))
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
st <- proc.time()
b <- crossprod(solve(crossprod(X)),crossprod(X,y))
end1 <- proc.time() - st
st <- proc.time()
b0    <- solve(t(X)%*%X)%*%t(X)%*%y
end2 <- proc.time() - st
max(abs(b0-b))
end1*10000
end2*10000
crossprod(resid)
(crossprod(resid))/(n-1)
sqrt((crossprod(resid))/(n-1))
n
con <- list(const = TRUE,
getSE = TRUE)
# Perform some checks for controls
nmsC <- names(con)
con[(namc <- names(control))] <- control
if(length(noNms <- namc[!namc %in% nmsC])){
warning("unknown names in control: ", paste(noNms,collapse=", "))
}
# ----- Get process dimensions
n <- nrow(Y)
q <- ncol(Y)
# ----- estimate model
if (con$const==TRUE){
mu <- colMeans(Y)
}else{
mu <- rep(0, q)
}
n
q
matrix(1, n, 1)
matrix(1, n, 1)%*%t(as.matrix(mu))
crosprod(matrix(1, n, 1),as.matrix(mu))
crossprod(matrix(1, n, 1),as.matrix(mu))
library(MSTest)
